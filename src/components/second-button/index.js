import { Link } from "react-router-dom";

export const SecondButton = ({ url }) => {
    return (
        <Link to={url} className="btn-third lusitana-regular text-center">REQUEST QUOTE</Link>
    );
} 